# 串口遥控指令

## 串口调试助手发送（HEX 模式，115200 8N1）

| 功能 | 十六进制命令 |
|---|---|
| 开始录像 | `EB 90 02 01 53 D1` |
| 结束录像 | `EB 90 02 01 54 D2` |
| 视频传输 | `EB 90 14 01 11 A1` |
| 单张拍照（F429兼容） | `EB 90 02 01 45 C3` |
| 批量拍照 N 张 | `EB 90 02 02 45 NN CS`，`CS = (EB+90+02+02+45+NN) & FF` |
| 图片批次回传（F429图片协议，经 USART1/RS422） | `EB 90 14 01 33 C3` |

`NN` 的范围为 1..255。例如拍 5 张为 `EB 90 02 02 45 05 C9`。遥控帧采用
长度字段：总长度 = `5 + Byte3`，故旧的单字节 payload 命令仍为 6 字节，批量拍照为 7 字节。

## Python 脚本发送（在 tools 目录下执行）

```
python uart_video_receiver.py COM8 start-rec
python uart_video_receiver.py COM8 stop-rec
python uart_video_receiver.py COM8 transfer
python uart_video_receiver.py COM8 photo --count 5
python uart_video_receiver.py COM8 photo-transfer
```
